/* =============================================
   raquel-hris-login.css
   Raquel Pawnshop HRIS — Login Page Styles
   =============================================
   Sections:
   1. Variables
   2. Reset & Base
   3. Page Layout
   4. HRIS Brand Panel (left)
   5. ESS Brand Panel (right)
   6. Form Panel & Cards
   7. Form Elements
   8. Buttons
   9. Animations
   10. Mobile (max 640px)
   11. Tablet (641px – 900px)
============================================= */


/* =============================================
   1. VARIABLES
============================================= */

:root {
  /* Brand greens */
  --green-dark:    #031b02;
  --green-forest:  #045202;
  --green-mid:     #1a2b04;
  --green-primary: #294306;
  --green-bright:  #28a745;

  /* Brand gold (used on brand panels only) */
  --gold:          #bd9414;
  --gold-light:    #ffd754;

  /* Text & UI */
  --text-dark:     #1a1a2e;
  --text-muted:    #6c757d;
  --text-light:    #9db0a3;

  /* Form surface */
  --input-bg:      #f8faf8;
  --input-border:  #e2ebe4;
  --input-hover:   #c8d8cb;

  /* Transition speed */
  --speed-fast:    0.2s;
  --speed-normal:  0.35s;
  --speed-slow:    0.58s;
}


/* =============================================
   2. RESET & BASE
============================================= */

*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html, body {
  height: 100%;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: var(--green-forest);
  -webkit-font-smoothing: antialiased;
}

input, button, select, textarea {
  font-family: inherit;
}


/* =============================================
   3. PAGE LAYOUT
============================================= */

/* Outer wrapper — fills the full viewport */
.root {
  width: 100%;
  height: 100vh;
  overflow: hidden;
  position: relative;
}

/* Sliding track that holds all screens */
.track {
  display: flex;
  width: 100%;
  height: 100vh;
  position: relative;
}

/* A single screen (HRIS or ESS view) */
.screen {
  display: flex;
  width: 100%;
  height: 100vh;
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
}


/* =============================================
   4. HRIS BRAND PANEL (left side, dark green)
============================================= */

.brand {
  width: 50%;
  position: absolute;
  top: 0;
  left: 0;
  bottom: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 48px 32px;
  background: linear-gradient(160deg, #0f2d0d 0%, var(--green-forest) 60%, var(--green-dark) 100%);
  overflow: hidden;
  z-index: 1;
  opacity: 1;
  transition: opacity 0.17s ease 0.23s;
}

/* Fade out HRIS brand when ESS is active */
.show-ess .brand {
  opacity: 0;
}

/* Subtle diagonal grid texture on the brand panel */
.brand::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background-image:
    repeating-linear-gradient(45deg,  transparent, transparent 28px, rgba(189,148,20,.04) 28px, rgba(189,148,20,.04) 29px),
    repeating-linear-gradient(-45deg, transparent, transparent 28px, rgba(189,148,20,.04) 28px, rgba(189,148,20,.04) 29px);
}

/* Corner bracket decorations */
.cr {
  position: absolute;
  width: 40px;
  height: 40px;
}

.tl { top: 18px;    left: 18px;  border-top:    1px solid rgba(189,148,20,.3); border-left:  1px solid rgba(189,148,20,.3); }
.tr { top: 18px;    right: 18px; border-top:    1px solid rgba(189,148,20,.3); border-right: 1px solid rgba(189,148,20,.3); }
.bl { bottom: 18px; left: 18px;  border-bottom: 1px solid rgba(189,148,20,.3); border-left:  1px solid rgba(189,148,20,.3); }
.br { bottom: 18px; right: 18px; border-bottom: 1px solid rgba(189,148,20,.3); border-right: 1px solid rgba(189,148,20,.3); }

/* Content block inside the brand panel */
.bcon {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: 20px;
  opacity: 0;
  transform: translateY(8px);
  transition: opacity 0.3s ease, transform 0.3s ease;
}

/* Show brand content when HRIS is active */
body:not(.show-ess) .bcon {
  opacity: 1;
  transform: translateY(0);
}

/* Logo badge */
.logo-ring {
  width: 100px;
  height: 100px;
  border-radius: 24px;
  background: #fff;
  border: 1.5px solid rgba(189,148,20,0.35);
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  box-shadow: 0 10px 30px rgba(0,0,0,0.25);
  transition: transform 0.4s ease, box-shadow 0.4s ease, border-color 0.4s ease;
  cursor: default;
}

.logo-ring:hover {
  transform: translateY(-4px) scale(1.03);
  border-color: var(--gold);
  box-shadow: 0 15px 35px rgba(189,148,20,0.2);
}

/* Glow ring behind the logo badge */
.logo-ring::before {
  content: '';
  position: absolute;
  top: -12px; left: -12px; right: -12px; bottom: -12px;
  border-radius: 30px;
  background: radial-gradient(circle, rgba(189,148,20,0.28) 0%, transparent 70%);
  z-index: -1;
  animation: pulseGlow 3s ease-in-out infinite;
  pointer-events: none;
}

.logo-ring img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  border-radius: 22px;
  padding: 8px;
}

/* Fallback letter if logo image fails to load */
.mono {
  font-size: 46px;
  font-weight: 600;
  color: var(--gold);
  line-height: 1;
  user-select: none;
}

/* "Human Resource Department" pill label */
.b-label {
  display: inline-block;
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--gold-light);
  background: rgba(189,148,20,0.1);
  border: 1px solid rgba(189,148,20,0.28);
  padding: 4px 12px;
  border-radius: 100px;
  margin-bottom: 8px;
}

/* Thin gold divider line */
.g-line {
  width: 60px;
  height: 2px;
  background: linear-gradient(90deg, transparent, var(--gold), transparent);
  margin: 12px auto;
}

/* Company name heading */
.b-name {
  font-size: 34px;
  font-weight: 800;
  color: #fff;
  line-height: 1.1;
  letter-spacing: -0.5px;
  text-shadow: 0 4px 12px rgba(0,0,0,0.3);
}

/* Tagline pill */
.b-tag {
  font-size: 15px;
  font-weight: 500;
  color: #fff;
  letter-spacing: 0.5px;
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(255,255,255,0.12);
  padding: 10px 22px;
  border-radius: 12px;
  max-width: 220px;
  margin-top: 5px;
}

.b-tag b {
  color: var(--gold-light);
}


/* =============================================
   5. ESS BRAND PANEL (right side, darker green)
============================================= */

.ess-brand {
  width: 50%;
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 48px 28px;
  background: linear-gradient(160deg, var(--green-primary) 0%, var(--green-mid) 100%);
  overflow: hidden;
  z-index: 1;
  opacity: 0;
  transition: opacity 0.17s ease 0.23s;
}

/* Show ESS brand when ESS is active */
.show-ess .ess-brand {
  opacity: 1;
}

/* Subtle diagonal grid texture */
.ess-brand::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background-image:
    repeating-linear-gradient(45deg,  transparent, transparent 26px, rgba(255,255,255,.03) 26px, rgba(255,255,255,.03) 27px),
    repeating-linear-gradient(-45deg, transparent, transparent 26px, rgba(255,255,255,.03) 26px, rgba(255,255,255,.03) 27px);
}

/* ESS corner brackets (white instead of gold) */
.ess-brand .tl { border-top:    1px solid rgba(255,255,255,.2); border-left:  1px solid rgba(255,255,255,.2); }
.ess-brand .tr { border-top:    1px solid rgba(255,255,255,.2); border-right: 1px solid rgba(255,255,255,.2); }
.ess-brand .bl { border-bottom: 1px solid rgba(255,255,255,.2); border-left:  1px solid rgba(255,255,255,.2); }
.ess-brand .br { border-bottom: 1px solid rgba(255,255,255,.2); border-right: 1px solid rgba(255,255,255,.2); }

/* Content block inside ESS brand panel */
.ess-bcon {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: 18px;
  opacity: 0;
  transform: translateY(8px);
  transition: opacity 0.3s ease, transform 0.3s ease;
}

/* Show ESS content when ESS is active */
.show-ess .ess-bcon {
  opacity: 1;
  transform: translateY(0);
}

/* ESS logo badge */
.ess-ring {
  width: 100px;
  height: 100px;
  border-radius: 24px;
  background: #fff;
  border: 1.5px solid rgba(255,255,255,0.25);
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  box-shadow: 0 10px 30px rgba(0,0,0,0.25);
  transition: transform 0.4s ease, box-shadow 0.4s ease, border-color 0.4s ease;
  cursor: default;
}

.ess-ring:hover {
  transform: translateY(-4px) scale(1.03);
  border-color: rgba(255,255,255,0.6);
  box-shadow: 0 15px 35px rgba(255,255,255,0.1);
}

.ess-ring::before {
  content: '';
  position: absolute;
  top: -12px; left: -12px; right: -12px; bottom: -12px;
  border-radius: 30px;
  background: radial-gradient(circle, rgba(255,255,255,0.18) 0%, transparent 70%);
  z-index: -1;
  animation: pulseGlow 3s ease-in-out infinite;
  pointer-events: none;
}

.ess-ring img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  border-radius: 22px;
  padding: 8px;
}

/* "Employee Portal" pill label */
.e-label {
  display: inline-block;
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: #fff;
  background: rgba(255,255,255,0.1);
  border: 1px solid rgba(255,255,255,0.22);
  padding: 4px 12px;
  border-radius: 100px;
  margin-bottom: 8px;
}

/* Thin white divider line */
.e-line {
  width: 60px;
  height: 2px;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.6), transparent);
  margin: 12px auto;
}

/* Company name */
.e-name {
  font-size: 34px;
  font-weight: 800;
  color: #fff;
  line-height: 1.1;
  letter-spacing: -0.5px;
  text-shadow: 0 4px 12px rgba(0,0,0,0.3);
}

/* Tagline pill */
.e-tag {
  font-size: 13.5px;
  color: rgba(255,255,255,0.9);
  line-height: 1.5;
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.1);
  padding: 10px 22px;
  border-radius: 12px;
  max-width: 220px;
  margin-top: 5px;
}

/* =============================================
   6. FORM PANEL & CARDS
============================================= */

/* Container that holds the login card — sits on the right half */
.form-panel {
  position: absolute;
  top: 0;
  left: 50%;
  right: 0;
  bottom: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 2;
  transition: transform var(--speed-slow) ease;
}

/* Slide the form panel left when ESS is active */
.show-ess .form-panel {
  transform: translateX(-100%);
}

/* The white login card */
.form-inner {
  width: 360px;
  background: #fff;
  border-radius: 20px;
  padding: 36px 32px 28px;
  border: 1px solid rgba(210,221,213,0.4);
  box-shadow:
    0 0 0 1px rgba(4,82,2,0.07),
    0 8px 24px rgba(4,82,2,0.14),
    0 32px 72px rgba(4,82,2,0.22);
  position: relative;
  z-index: 1;
  transition: opacity var(--speed-normal) ease;
}

/* Thin colored line at the top of each card */
.hris::before,
.essf::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3px;
  border-radius: 20px 20px 0 0;
}

.hris::before {
  background: linear-gradient(90deg, transparent, var(--green-forest), #1a5c18, var(--green-forest), transparent);
}

.essf::before {
  background: linear-gradient(90deg, transparent, var(--green-bright), #34c059, var(--green-bright), transparent);
}

/* HRIS card — visible by default */
.hris {
  opacity: 1;
  pointer-events: auto;
}

.show-ess .hris {
  opacity: 0;
  pointer-events: none;
}

/* ESS card — hidden by default, shown when ESS is active */
.essf {
  opacity: 0;
  pointer-events: none;
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  margin: auto;
  width: 360px;
  height: fit-content;
  transition: opacity var(--speed-normal) ease;
}

.show-ess .essf {
  opacity: 1;
  pointer-events: auto;
}

/* Mobile-only elements are hidden on desktop */
.mob-tabs {
  display: none;
}


/* =============================================
   7. FORM ELEMENTS
============================================= */

/* Small badge above the form title e.g. "HRIS Portal" */
.eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-size: 8.5px;
  font-weight: 700;
  letter-spacing: 2.5px;
  text-transform: uppercase;
  padding: 4px 10px;
  border-radius: 6px;
  margin-bottom: 12px;
  color: var(--green-primary);
  background: rgba(41,67,6,0.1);
  border: 1px solid rgba(41,67,6,0.25);
}

/* Form heading */
.form-title {
  font-size: 34px;
  font-weight: 600;
  line-height: 1.05;
  letter-spacing: -0.5px;
  margin-bottom: 6px;
  color: #141f17;
}

/* Subtitle below the heading */
.form-sub {
  font-size: 12.5px;
  color: #8a9e90;
  line-height: 1.5;
  margin-bottom: 18px;
  padding-bottom: 14px;
  border-bottom: 1px solid rgba(4,82,2,0.07);
}

/* Error message box */
.login-error {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  background: rgba(220,53,69,0.06);
  border: 1px solid rgba(220,53,69,0.22);
  border-left: 3px solid #dc3545;
  border-radius: 10px;
  padding: 12px 14px;
  margin-bottom: 18px;
  font-size: 13px;
  color: #b91c1c;
  line-height: 1.45;
}

.login-error i {
  font-size: 16px;
  flex-shrink: 0;
  margin-top: 1px;
}

/* Field wrapper */
.field {
  margin-bottom: 12px;
}

.field label {
  display: block;
  font-size: 9.5px;
  font-weight: 700;
  letter-spacing: 1.2px;
  text-transform: uppercase;
  color: #4a6352;
  margin-bottom: 6px;
}

/* Input wrapper — holds icon + input + eye button */
.iwrap {
  position: relative;
  display: flex;
  align-items: center;
}

/* Left icon inside the input */
.iico {
  position: absolute;
  left: 13px;
  font-size: 15px;
  color: #b8ccbe;
  pointer-events: none;
  transition: color var(--speed-fast);
}

/* Text input */
.iwrap input {
  width: 100%;
  padding: 12px 14px 12px 40px;
  background: var(--input-bg);
  border: 1.5px solid var(--input-border);
  border-radius: 10px;
  font-size: 13.5px;
  color: var(--text-dark);
  outline: none;
  transition: background var(--speed-fast), border-color var(--speed-fast), box-shadow var(--speed-fast);
}

.iwrap input:hover {
  background: #f3f7f4;
  border-color: var(--input-hover);
}

.iwrap input::placeholder {
  color: #bfcfc4;
  font-weight: 300;
}

/* Focus styles — HRIS card (green) */
.hris .iwrap input:focus {
  background: #fff;
  border-color: var(--green-forest);
  box-shadow: 0 0 0 4px rgba(4,82,2,0.12);
}

.hris .iwrap:focus-within .iico {
  color: var(--green-forest);
}

/* Focus styles — ESS card (darker green) */
.essf .iwrap input:focus {
  background: #fff;
  border-color: var(--green-primary);
  box-shadow: 0 0 0 4px rgba(41,67,6,0.12);
}

.essf .iwrap:focus-within .iico {
  color: var(--green-primary);
}

/* Show/hide password button */
.eyebtn {
  position: absolute;
  right: 12px;
  background: none;
  border: none;
  cursor: pointer;
  color: var(--text-light);
  font-size: 16px;
  padding: 4px;
  display: flex;
  align-items: center;
  border-radius: 6px;
  transition: color var(--speed-fast), background var(--speed-fast);
}

.eyebtn:hover               { background: rgba(0,0,0,0.04); }
.hris .eyebtn:hover         { color: var(--green-forest); }
.essf .eyebtn:hover         { color: var(--green-bright); }

/* Row with "Remember me" and "Forgot password" */
.optrow {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20px;
}

.rmb {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 12.5px;
  color: #6a7d6f;
  cursor: pointer;
  user-select: none;
}

.rmb input {
  width: 14px;
  height: 14px;
  cursor: pointer;
}

.hris .rmb input { accent-color: var(--green-forest); }
.essf .rmb input { accent-color: var(--green-primary); }

/* Forgot password link */
.fgt {
  font-size: 12.5px;
  font-weight: 500;
  text-decoration: none;
  transition: opacity var(--speed-fast);
}

.hris .fgt { color: var(--green-forest); }
.essf .fgt { color: var(--green-primary); }
.fgt:hover { opacity: 0.65; }

/* Footer copyright line */
.ffoot {
  text-align: center !important;
  font-size: 11px !important;
  color: #3d4d42 !important;
  letter-spacing: 0.3px;
  padding-top: 16px !important;
  border-top: 1px solid rgba(4,82,2,0.15) !important;
  font-weight: 500 !important;
}

/* =============================================
   8. BUTTONS
============================================= */

/* Primary "Sign In" button */
.btn-p {
  width: 100%;
  padding: 12px 20px;
  border: none;
  border-radius: 11px;
  font-size: 11.5px;
  font-weight: 700;
  letter-spacing: 1.8px;
  text-transform: uppercase;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 9px;
  margin-bottom: 10px;
  position: relative;
  overflow: hidden;
  transition: transform var(--speed-fast), box-shadow var(--speed-fast);
}

/* Subtle shine overlay on the button */
.btn-p::after {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background: linear-gradient(180deg, rgba(255,255,255,0.18) 0%, transparent 60%);
  pointer-events: none;
}

.btn-p:hover  { transform: translateY(-2px); }
.btn-p:active { transform: translateY(0); }

/* HRIS Sign In — forest green */
.btn-gold {
  background: linear-gradient(135deg, #1a5c18 0%, var(--green-forest) 50%, var(--green-dark) 100%);
  color: #fff;
  box-shadow: 0 4px 14px rgba(4,82,2,0.35);
}

.btn-gold:hover {
  box-shadow: 0 8px 24px rgba(4,82,2,0.5);
}

/* ESS Sign In — darker green */
.btn-green {
  background: linear-gradient(135deg, var(--green-primary) 0%, var(--green-mid) 100%);
  color: #fff;
  box-shadow: 0 4px 14px rgba(41,67,6,0.3);
}

.btn-green:hover {
  box-shadow: 0 8px 24px rgba(41,67,6,0.45);
}

/* Secondary "Switch portal" button */
.btn-s {
  width: 100%;
  padding: 10px 20px;
  background: transparent;
  border: 1.5px solid #dde8df;
  border-radius: 11px;
  font-size: 12.5px;
  font-weight: 500;
  font-style: normal;
  color: #607868;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 9px;
  margin-bottom: 18px;
  transition: all var(--speed-fast);
}

.hris .btn-s:hover {
  border-color: var(--green-forest);
  color: var(--green-forest);
  background: rgba(4,82,2,0.05);
  transform: translateY(-1px);
}

.essf .btn-s:hover {
  border-color: var(--green-primary);
  color: var(--green-primary);
  background: rgba(41,67,6,0.05);
  transform: translateY(-1px);
}


/* =============================================
   9. ANIMATIONS
============================================= */

/* Pulsing glow behind logo badges */
@keyframes pulseGlow {
  0%, 100% { opacity: 0.55; transform: scale(1); }
  50%       { opacity: 1;    transform: scale(1.08); }
}

/* Mobile fade in for brand header */
@keyframes mobileFadeUp {
  from { opacity: 0; transform: translate3d(0, 10px, 0); }
  to   { opacity: 1; transform: translate3d(0, 0, 0); }
}

/* Mobile fade in for login card */
@keyframes mobileCardIn {
  from { opacity: 0; transform: translate3d(0, 12px, 0); }
  to   { opacity: 1; transform: translate3d(0, 0, 0); }
}


/* =============================================
   10. MOBILE  (max-width: 640px)
   Layout: scrollable page with tall brand header.
============================================= */

@media (max-width: 640px) {

  /* Scrollable page */
  html, body {
    height: auto;
    min-height: 100vh;
    overflow-y: auto;
    background: linear-gradient(160deg, #0f2d0d 0%, var(--green-forest) 60%, var(--green-dark) 100%);
  }

  .root {
    height: auto;
    min-height: 100vh;
    overflow: visible;
  }

  .track {
    height: auto;
    min-height: 100vh;
    display: block;
    position: static;
  }

  .screen {
    position: static;
    display: flex;
    flex-direction: column;
    width: 100%;
    height: auto;
    min-height: 100vh;
  }

  /* Brand header — tall centered column (same as before) */
  .brand,
  .ess-brand {
    position: static;
    width: 100%;
    height: auto;
    padding: 44px 24px 32px;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transition: none;
    opacity: 1;
    animation: mobileFadeUp 0.35s ease both;
  }

  /* Show/hide brand panels by toggling display */
  .brand                { display: flex; }
  .ess-brand            { display: none; }
  .show-ess .brand      { display: none; }
  .show-ess .ess-brand  { display: flex; }

  /* Brand content */
  .bcon,
  .ess-bcon {
    gap: 12px;
    opacity: 1;
    transform: none;
    transition: none;
  }

  /* Logo sizes */
  .logo-ring,
  .ess-ring {
    width: 72px;
    height: 72px;
  }

  .logo-ring,
  .ess-ring {
    border-radius: 14px;
  }

  .logo-ring img,
  .ess-ring img {
    border-radius: 10px;
    padding: 6px;
  }

  /* Company name sizes */
  .b-name,
  .e-name {
    font-size: 24px;
  }

  .e-name span {
    font-size: 18px;
  }

  .b-tag,
  .e-tag {
    font-size: 12px;
  }

  /* Form panel */
  .form-panel,
  .show-ess .form-panel {
    position: static;
    left: auto;
    right: auto;
    top: auto;
    bottom: auto;
    width: 100%;
    height: auto;
    padding: 0;
    background: transparent;
    box-shadow: none;
    display: block;
    border-radius: 28px 28px 0 0;
    margin-top: 0;
    transform: none;
    transition: none;
    z-index: 10;
  }

  /* Login cards */
  .form-inner.hris,
  .form-inner.essf {
    position: static;
    width: 100%;
    max-width: 100%;
    border-radius: 28px 28px 0 0;
    padding: 32px 20px 40px;
    box-shadow: 0 -8px 40px rgba(4,82,2,0.22);
    min-height: calc(100vh - 220px);
    opacity: 1;
    pointer-events: auto;
    transform: none;
    animation: mobileCardIn 0.35s ease both;
    inset: auto;
    margin: 0;
    height: auto;
  }

  @supports (height: 100svh) {
    .form-inner.hris,
    .form-inner.essf {
      min-height: calc(100svh - 220px);
    }
  }

  /* Show/hide cards */
  .form-inner.essf               { display: none; }
  .show-ess .form-inner.hris     { display: none; }
  .show-ess .form-inner.essf     { display: block; }

  /* Mobile tab switcher */
  .mob-tabs {
    display: flex;
    gap: 8px;
    margin-bottom: 24px;
  }

  .mob-tab {
    flex: 1;
    padding: 10px 8px;
    border-radius: 10px;
    border: 1.5px solid #e4ebe6;
    background: transparent;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 1px;
    text-transform: uppercase;
    color: #7a8a7f;
    cursor: pointer;
    transition: all var(--speed-fast);
  }

  .mob-tab.active-gold {
    background: rgba(4,82,2,0.08);
    border-color: var(--green-forest);
    color: var(--green-forest);
  }

  .mob-tab.active-green {
    background: rgba(41,67,6,0.08);
    border-color: var(--green-primary);
    color: var(--green-primary);
  }

  /* Hide desktop switch button on mobile */
  .btn-s {
    display: none;
  }

  /* Tighter spacing */
  .form-title {
    font-size: 32px;
  }

  .form-sub {
    margin-bottom: 16px;
    padding-bottom: 14px;
  }

  .field {
    margin-bottom: 11px;
  }

  .optrow {
    margin-bottom: 14px;
  }

  .btn-p {
    padding: 14px 20px;
  }
}


/* =============================================
   11. TABLET  (641px – 900px)
============================================= */

@media (min-width: 641px) and (max-width: 900px) {

  .brand,
  .ess-brand {
    width: 50%;
  }

  .form-panel {
    left: 50%;
  }

  .show-ess .form-panel {
    transform: translateX(-100%);
  }

  .form-inner {
    padding: 28px 24px 22px;
    max-width: 360px;
  }

  .essf {
    max-width: 360px;
  }

  .form-title {
    font-size: 30px;
  }
}
